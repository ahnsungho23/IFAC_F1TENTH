numpy
opencv-python
PyYAML
scipy
scikit-image
quadprog

# Optional, only for `--optimizer laptime` (differentiable GPU lap-time optimizer):
# install ONE of the following.
#   torch                      # Ubuntu / car PC (CUDA GPU used automatically)
#   mlx; sys_platform=="darwin" and platform_machine=="arm64"   # Apple Silicon macOS (Metal)
